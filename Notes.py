#时间复杂度的计算
#   最少需要多少基本操作——最优时间复杂度
#   最多需要多少基本操作——最坏时间复杂度
#   平均需要多少基本操作——平均时间复杂度
#   运算规则：
#       顺序结构——加法
#       循环结构——乘法
#       分支结构——时间复杂度取最大值
#   大小关系：O(1)<O(logn)<O(n)<O(n^2)<O(n^3)<O(2^n)<O(n!)<O(n^n)

#Python内置类型性能分析模块——timeit

#列表各种方法的时间复杂度
#O(1):
#   indexx[]
#   index assignment
#   append
#   pop()
#O(n):
#   pop(n)
#   insert(i,item)
#   del operator
#   iteration
#   contains(in)
#   reverse
# O(k)：
#   get slice[x:y]
#  O(k+n):
#   set slice
# O(nlogn):
#   sort
# O(nk):
#   multiply








